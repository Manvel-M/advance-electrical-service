---
import MobileNav from "./MobileNav.astro";
import Nav from "./Nav.astro";
---

<header
  id="site-header"
  class="fixed w-full h-20 top-0 left-0 bg-surface text-surface-foreground shadow-lg flex items-center justify-between px-4 z-50 transition-all duration-300 ease-in-out"
>
  <div class="logo">
    <a href="/" class="text-xl font-bold">Advance Electrical Service</a>
  </div>
  <Nav />
  <MobileNav />
</header>

<script is:inline>
  let lastScrollY = window.scrollY;
  const header = document.getElementById("site-header");

  const updateHeader = () => {
    const currentScroll = window.scrollY;

    // Show/hide logic based on scroll direction and position
    if (currentScroll > lastScrollY && currentScroll > 100) {
      // Scrolling down past 100px
      header.classList.add("-translate-y-full");
    } else if (currentScroll < lastScrollY) {
      // Scrolling up
      header.classList.remove("-translate-y-full");
    }

    lastScrollY = currentScroll;
  };

  let ticking = false;
  window.addEventListener("scroll", () => {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        updateHeader();
        ticking = false;
      });
      ticking = true;
    }
  });
</script>
